<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "My Garage01 " %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <!-- application.html.erb is a wrapper file forevery page within the project -->
    <!-- Start Top Bar will appear in every page within the project -->
    <div class="top-bar mg-top-bar ">
      <div class="top-bar-left">
        <ul class="menu float-left mg-topbrand" >
          <li class="menu-text mg-topbrandli">My Garage</li>
        </ul>

        <ul class="menu float-right mg-topmenu" >
        <% if user_signed_in? %>
          <li class=" menu-text float-right mg-topmenuli"> 
            <%= current_user.email %>
            <%= link_to "Log Out", destroy_user_session_path, method: 'DELETE' %> 
          </li>
        <% else %>
          <li class=" menu-text  float-right mg-topmenuli">  
          <%= link_to "Login", new_user_session_path %> 
          or
          <%= link_to "Sign Up", new_user_registration_path %>  
          </li>
        <% end %>
        </ul>
      </div>
    </div>
    <!-- End Top Bar -->

    <div class="callout large primary mg-jumbo">
      <div class="row column text-center">
        <h5 class="subheader">Welcome to the</h5>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    </div>

    <div class="row medium-8 large-7 columns">
      <div class="blog-post">
        <div class="mg-yieldcontent">
          <!-- <p>Rails dynamic content displays here. </p> -->
          <%= yield %>
          <!-- <p>Rails dynamic content ends here. </p> -->
        </div>
      </div>
    </div>

      <!--  these js files came with the Foundation starter template, they need to be added to assets/javascript/vendors  -->
    <script src="https://code.jquery.com/jquery-2.1.4.min.js"></script>
    <script src="https://dhbhdrzi4tiry.cloudfront.net/cdn/sites/foundation.js"></script>
    <script>
    $(document).foundation();
    </script>
  </body>
</html>